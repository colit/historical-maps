enum ViewState {
  idle,
  busy,
}

enum LoadingState {
  idle,
  busy,
  progress,
}

enum PageType {
  startup,
  onboarding,
  locationRequest,
  notificationsRequest,
  shell,
  map,
  notes,
  info,
}
