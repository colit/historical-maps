enum ViewState {
  idle,
  busy,
}

enum LoadingState {
  idle,
  install,
  progress,
}

enum PageType {
  startup,
  onboarding,
  locationRequest,
  notificationsRequest,
  shell,
  map,
  notes,
  info,
  photoDetails,
}
